# SMTP configuration for local Tomcat
# Priority: System.getProperty → mail.properties → System.getenv → default
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USE_SSL=false
SMTP_USERNAME=omnicoreshop@gmail.com
SMTP_PASSWORD=bbtk nqyu jwxc ofqj
SMTP_DEBUG=false

# SECURITY: Set via environment variable SMTP_PASSWORD instead of hardcoding here.
# Export before starting Tomcat: export SMTP_PASSWORD="your_app_password"
